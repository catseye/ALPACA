/*
pf = (new yoob.Playfield()).init({defaultValue:'Dead'});
pf.putDirty(2, 0, 'Alive');
pf.putDirty(0, 1, 'Alive');
pf.putDirty(2, 1, 'Alive');
pf.putDirty(1, 2, 'Alive');
pf.putDirty(2, 2, 'Alive');
pf.recalculateBounds();

newPf = (new yoob.Playfield()).init({defaultValue:'Dead'});
evolve_playfield(pf, newPf);
console.log('-----');
console.log(newPf.dump(dumpMapper).replace(/\n$/, ""));
console.log('-----');
*/
